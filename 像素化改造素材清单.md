# 架构游戏 · 像素化改造素材清单

将最后一幕「工作量仿真架构游戏」改造成成熟的像素游戏界面（类似植物大战僵尸的视角与操作分割），以下是需要准备的图片素材清单。

## 目标布局（PvZ 式操作分割）

| 区域 | 现状 | 改造后 |
|---|---|---|
| 顶部 | 文字标题 + 金币 + 卡片条 | PvZ 阳光槽 + 卡片栏（选卡 → 点战场放置） |
| 中间 | 矩形区域框 + 卡槽 | 像素战场：横向主干 5 块地皮（数据库 / 后端 / 缓存 / 网关 / 用户），地面用像素地块铺 |
| 右上 | 指标面板 | 像素 HUD 小面板（QPS / 在线用户 / 金币） |
| 底部 | 控件面板 | 底部操作条：量级滑块、请求开关、缓存命中、突发 / 重置 |
| 弹窗 | 白色对话框 | 像素风弹窗（节点详情 / 网关配置） |

## 素材清单

### 1. 背景与战场地块（替换所有矩形区域框）

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `bg_lawn.png` | 主战场整幅背景（像素草地 / 机房地面） | 1920×740 或可平铺 256×256 |
| `tile_db.png` | 数据库区地块（紫色系） | 64×64 平铺块 |
| `tile_backend.png` | 后端区地块（绿色系） | 64×64 |
| `tile_cache.png` | 缓存旁路区地块（蓝色系，位于后端正上方） | 64×64 |
| `tile_gateway.png` | 网关区地块（橙色系） | 64×64 |
| `tile_user.png` | 用户生成区地块（灰色系） | 64×64 |

### 2. 单位精灵（替换 db / cache / backend / gateway 的矩形色块）

每个节点 1 个精灵，建议动画帧 spritesheet（2~4 帧循环，待机 / 工作闪烁）。

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `node_db.png` | 数据库单位 | 96×96，2~4 帧 |
| `node_cache.png` | 缓存服务器单位 | 96×96 |
| `node_backend.png` | 后端服务器单位 | 96×96 |
| `node_gateway.png` | 网关单位 | 96×96 |
| `node_fail.png` | 宕机状态遮罩 / 变灰变体（可选） | 96×96 |

> 高负载红光、升级徽标、宕机标识等逻辑层叠加由代码特效实现，无需单独出图。

### 3. 卡片（替换顶部卡片栏，PvZ 种子包样式）

每张卡 1 张底图 + 状态变体（价格、锁定时灰态，可做在同一张 spritesheet）。

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `card_db.png` | 数据库卡片（含价格、锁定变体） | 80×100 |
| `card_cache.png` | 缓存卡片 | 80×100 |
| `card_backend.png` | 后端卡片 | 80×100 |
| `card_gateway.png` | 网关卡片 | 80×100 |
| `card_highlight.png` | 选中 / 悬停高亮框 | 80×100 |

### 4. 卡槽 / 放置格（替换 `+` 占位）

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `slot_empty.png` | 空卡槽 | 匹配地块 64×64 |
| `slot_target.png` | 拖拽时合法高亮 | 64×64 |

### 5. 用户单位（替换灰色圆点）

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `user_idle.png` | 用户小精灵（2~3 帧走动 / 待机） | 32×32 |

### 6. 特效（替换发光折线 / 光点 / 悬浮文字）

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `ray_cyan.png` | 用户 → 网关 射线段 | 细长 8×16（或代码绘制，可选） |
| `ray_yellow.png` | 网关 → 后端 射线段 | 同上 |
| `ray_blue.png` | 后端 → 缓存（命中） | 同上 |
| `ray_purple.png` | 后端 → 数据库 | 同上 |
| `dot_head.png` | 射线头部光点 | 16×16 |
| `float_bubble.png` | 请求类型悬浮文字气泡 | 32×20 |
| `fx_red_glow.png` | 高负载红光柔光罩 | 128×128 半透明 |

### 7. HUD / 面板 / 按钮（替换顶部栏、指标框、底部控件、按钮）

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `hud_top.png` | 顶部资源栏底图 | 1920×70 或平铺 256×32 |
| `hud_gold.png` | 金币图标 + 计数框 | 48×48 + 框 |
| `hud_qps.png` | 理论 / 真实 QPS 图标 | 32×32 |
| `hud_users.png` | 在线用户图标 | 32×32 |
| `hud_metric_frame.png` | 右上指标面板框 | 220×120 |
| `btn.png` | 通用按钮（悬停 / 禁用变体） | 80×32 spritesheet |
| `slider.png` | 滑块轨道 + 把手 | 轨道 200×16，把手 24×24 |
| `switch.png` | 请求类型开关（on / off） | 40×24 |

### 8. 弹窗（节点详情 / 网关配置）

| 素材 | 用途 | 建议规格 |
|---|---|---|
| `modal_frame.png` | 弹窗底图（九宫格可拉伸） | 64×64 九宫格 |
| `modal_title.png` | 弹窗标题栏 | 64×24 |
| `btn_close.png` | 关闭按钮 | 32×32 |
| `btn_upgrade.png` | 升级按钮（含禁用态） | 160×40 |

### 9. 小图标（详情弹窗属性行、请求类型）

| 素材 | 用途 |
|---|---|
| `icon_cpu.png` / `icon_mem.png` | 详情弹窗 CPU / 内存 折线图标题图标 |
| `icon_req_get.png` | 请求类型 get 图标 |
| `icon_req_list.png` | 请求类型 list 图标 |
| `icon_req_save.png` | 请求类型 save 图标 |
| `icon_req_calc.png` | 请求类型 calculate 图标 |
| `icon_req_download.png` | 请求类型 download 图标 |

## 交付约定

- 格式：PNG 透明背景；动画帧按「横向 spritesheet」一张出图
- 建议像素风格按 16 / 32 网格绘制；尺寸给推荐值即可，代码侧用 `nearest` 缩放保证像素锐利
- 文件放入 `src/`，用现有 `convert_img.py` 转成 base64 嵌入，保持单文件离线可运行
