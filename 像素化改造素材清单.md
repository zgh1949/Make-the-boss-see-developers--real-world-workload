# 架构游戏 · 像素化改造素材清单

将最后一幕「工作量仿真架构游戏」改造成成熟的像素游戏界面（PvZ 式视角与操作分割）。UI 布局以 `ui.json`（1024×768 设计稿）为基准，等比拉伸映射到 **1920×1080** 游戏画布。

## 目标布局（PvZ 式操作分割，1920×1080）

| 区域 | ui.json 源坐标(1024×768) | 映射后(1920×1080) | 内容 |
|---|---|---|---|
| 卡片栏 | x[34,379] y[71,187] | x[64,711] y[100,263] | 顶部左侧：4 张部署卡片（网关/后端/缓存/数据库） |
| 数据库区 | x[36,197] y[327,510] | x[68,369] y[460,717] | 战场区域框（紫色） |
| 缓存区域 | x[304,480] y[206,294] | x[570,900] y[290,413] | 战场区域框（蓝色，后端上方） |
| 后端区域 | x[291,516] y[384,568] | x[546,968] y[540,799] | 战场区域框（绿色） |
| 网关区域 | x[624,744] y[389,475] | x[1170,1395] y[547,668] | 战场区域框（橙色） |
| 用户区域 | x[868,983] y[202,541] | x[1628,1843] y[284,761] | 战场区域框（灰色，右侧） |
| 说明区域 | x[8,313] y[594,763] | x[15,587] y[835,1073] | 左下：操作提示面板 |
| 状态+业务滑块 | x[317,741] y[568,763] | x[594,1389] y[799,1073] | 底部中：用户量级 / 缓存命中滑块 + 突发 / 重置 |
| 请求类型开关 | x[748,1011] y[593,763] | x[1402,1896] y[834,1073] | 右下：5 个请求开关 |
| QPS 用户数 | x[339,719] y[580,645] | x[636,1348] y[816,907] | 底部中上：核心指标面板 |
| 关闭按钮 | — | (1880,20) | 右上角返回教程 |

## 素材清单

> 全部素材为 **PNG 透明背景**，生成后经 base64 嵌入 `WIMG` 对象（单文件离线运行）。代码侧统一用 `nearest` 缩放保证像素锐利。

### 1. 背景与战场地块

| 素材 | 用途 | 规格 |
|---|---|---|
| `bg_lawn.png` | 主战场整幅背景（像素草地/机房地面） | 256×256 可平铺 |
| `tile_db.png` | 数据库区地块（紫色系） | 64×64 |
| `tile_backend.png` | 后端区地块（绿色系） | 64×64 |
| `tile_cache.png` | 缓存旁路区地块（蓝色系） | 64×64 |
| `tile_gateway.png` | 网关区地块（橙色系） | 64×64 |
| `tile_user.png` | 用户生成区地块（灰色系） | 64×64 |

### 2. 单位精灵（3D 等距机柜）

等距透视机柜：金属渐变光泽、发光 LED 阵列、品牌条、通风口、硬盘槽、底部投影。主体居中，透明背景。

| 素材 | 用途 | 规格 |
|---|---|---|
| `node_db.png` | 数据库机柜（薰衣草紫） | 96×96 |
| `node_cache.png` | 缓存机柜（天蓝） | 96×96 |
| `node_backend.png` | 后端机柜（浅薄荷绿，参考 dev.png 明亮低饱和） | 96×96 |
| `node_gateway.png` | 网关机柜（暖杏色） | 96×96 |

> 宕机变灰（`tint`）、高负载红光（`fx_red_glow`）、升级徽标等逻辑层由代码实现，无需单独出图。

### 3. 卡片（PvZ 种子包样式）

| 素材 | 用途 | 规格 |
|---|---|---|
| `card_db.png` | 数据库卡片 | 80×100（显示放大到 138×138） |
| `card_cache.png` | 缓存卡片 | 80×100 |
| `card_backend.png` | 后端卡片 | 80×100 |
| `card_gateway.png` | 网关卡片 | 80×100 |
| `card_highlight.png` | 选中/悬停高亮框 | 80×100 |

### 4. 卡槽 / 放置格

| 素材 | 用途 | 规格 |
|---|---|---|
| `slot_empty.png` | 空卡槽 | 64×64 |
| `slot_target.png` | 拖拽时合法高亮 | 64×64 |

### 5. 用户单位

| 素材 | 用途 | 规格 |
|---|---|---|
| `user_idle.png` | 用户小精灵（待机） | 32×32（显示放大到 20×20） |

### 6. 特效

| 素材 | 用途 | 规格 |
|---|---|---|
| `ray_cyan.png` | 用户→网关 射线段 | 8×16 |
| `ray_yellow.png` | 网关→后端 射线段 | 8×16 |
| `ray_blue.png` | 后端→缓存（命中） | 8×16 |
| `ray_purple.png` | 后端→数据库 | 8×16 |
| `dot_head.png` | 射线头部光点 | 16×16 |
| `float_bubble.png` | 请求类型悬浮文字气泡 | 32×20 |
| `fx_red_glow.png` | 高负载红光柔光罩 | 128×128 半透明 |

### 7. HUD / 面板 / 按钮

| 素材 | 用途 | 规格 |
|---|---|---|
| `hud_gold.png` | 金币图标 | 48×48 |
| `hud_qps.png` | 理论 QPS 图标 | 32×32 |
| `hud_users.png` | 在线用户图标 | 32×32 |
| `hud_metric_frame.png` | 指标面板框 | 220×120 |
| `btn.png` | 通用按钮 | 80×32 |
| `slider.png` | 滑块轨道 + 把手 | 轨道 200×16，把手 24×24 |
| `switch.png` | 请求类型开关（on/off） | 40×24 |

### 8. 弹窗（节点详情 / 网关配置）

| 素材 | 用途 | 规格 |
|---|---|---|
| `modal_frame.png` | 弹窗底图（九宫格） | 64×64 |
| `modal_title.png` | 弹窗标题栏 | 64×24 |
| `btn_close.png` | 关闭按钮 | 32×32 |
| `btn_upgrade.png` | 升级按钮（含禁用态） | 160×40 |

### 9. 小图标

| 素材 | 用途 |
|---|---|
| `icon_cpu.png` / `icon_mem.png` | 详情弹窗 CPU / 内存折线图标题 |
| `icon_req_get.png` | 请求类型 get |
| `icon_req_list.png` | 请求类型 list |
| `icon_req_save.png` | 请求类型 save |
| `icon_req_calc.png` | 请求类型 calculate |
| `icon_req_download.png` | 请求类型 download |

## 绘制规范（内容占比 / 透明边）

按画布规格绘制素材时，真实内容占画布的比例与四周透明边的留法遵循以下规则：

| 素材类型 | 内容占比 | 透明边 | 原因 |
|---|---|---|---|
| 地块 tile（64×64） | 100% 铺满 | 0 | 无缝平铺，边框就是接缝，不能留白 |
| 单位/角色（96×96 机柜、32×32 小人） | 70~80% | 四周各留 10~15% | 给阴影/描边/抖动动画留空间，避免相邻对象视觉串扰 |
| 图标（32×32） | 60~75% | 四周各留 12~20% | 太满显拥挤，太稀显小气 |
| 卡片/按钮/面板（80×100 等） | 90~95% | 四角轻微圆角透明 | 主体是完整矩形 UI，仅四角可透明 |
| 光点/光晕特效（16×16 / 128×128） | 中心 40~50%，外围渐隐 | 光效靠衰减，外围必然是透明渐变 | 特效本质是"中间实、四周虚" |

### 本清单各素材的落地参考

| 素材 | 画布 | 内容建议 | 说明 |
|---|---|---|---|
| `tile_*.png` | 64×64 | 画满 | 边缘颜色必须能无缝对接（上下/左右同色） |
| `node_*.png` | 96×96 | ~70×70 居中 | 四周留 ~13px，落地投影与抖动不被裁 |
| `user_idle.png` | 32×32 | ~22×22 居中 | |
| `card_*.png` | 80×100 | ~76×96 | 四角留 2px 圆角 |
| `dot_head.png` | 16×16 | 光心 ~8×8，外圈渐隐 | |
| `icon_req_*.png` | 32×32 | ~22×22 居中 | |

> 内容占比只影响构图与透明边，**不影响最终显示大小**——游戏代码按 `width/height` 拉伸显示，界面尺寸由代码决定，而非画布内占比。

## 技术说明

- 画布 1920×1080，PixiJS 渲染；所有 UI（卡片栏/滑块/开关/指标/说明区/弹窗）全部在 PixiJS 内绘制，无 HTML 覆盖层
- 卡片点击 → 拖拽 → 高亮合法区域 → 松开部署；节点点击 → 详情/网关配置弹窗
- 素材 base64 嵌入 `WIMG` 对象；新增/替换素材后需重新生成 `WIMG` 并替换进 `index.html`
